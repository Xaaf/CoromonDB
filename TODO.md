# Future To-Do
> This file contains a bunch of to-do items. For the most part, these are to-do items for the future, so should-have and could-have. None of these are important for the MVP/initial release candidates.

## Coromon
For this, we might need to add new columns to the database's tables. These are marked with **DB**.
- [ ] Coromon Details (`coromon.lua` contains more info than currently is used)
    - [ ] **DB** Add descriptions
    - [ ] **DB** Add skill property tags
    - [ ] **DB** Add properties
    - [x] Add images; consider where to store them for long-term use!

## Traits
For this, we might need to add new columns to the database's tables. These are marked with **DB**.
- [ ] Trait details (`traits.lua` contains more info than currently is used)
    - [ ] **DB** Add cooldowns for active skills