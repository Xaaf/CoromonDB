export default function DataNotice() {
    return (
        <div className="max-w-3xl mx-auto p-4 mt-8 bg-(--color-bg-main-container) text-center shadow-lg/30 rounded-lg">
            {/* TODO: Add I icon */}
            <strong>Note</strong> The database is currently in development, so we might miss information!
        </div>
    );
}